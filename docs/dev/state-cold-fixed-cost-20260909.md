# 状态冷化：大清单缓存与批次固定成本

本轮针对状态积压重新增长，保留全部历史和当前数据库，不改变冷文件格式、历史查询语义、内容校验、发布后裁剪顺序或共享维护 gate 的份额。代码、本地回归、服务器原生验证及部署已完成。完整线上窗口确认速度改善，但净积压仍略增，本轮尚不能认定持续追赶已解决。

## 部署前基线

2026-09-09 北京时间 16:19:29–16:24:29，同一 `22972031` 进程，21/21 点、299.954 秒。原始 metrics 与 observations、summary 经独立逐项核算。

| 指标 | 起点 | 终点 | 变化及速率 |
|---|---:|---:|---:|
| 同步 head | 15,502,737 | 15,534,168 | +31,431，104.786 块/秒 |
| 状态发布及裁剪 | 14,163,960 | 14,169,093 | +5,133，17.113 块/秒 |
| head − 状态发布 | 1,338,777 | 1,365,075 | **增加 26,298 块** |
| 正文 coverage | 14,155,776 | 14,155,776 | 没有完成整段 |
| 索引 coverage/pruned | 11,452,416 | 11,575,296 | +122,880 |
| 正文 − 索引裁剪 | 2,703,360 | 2,580,480 | 减少 122,880 块 |

状态共完成 25 个强制批次，平均约 205 块/批。清单缓存 hits 和 resident 全程为零；miss 增加 439、bypass 增加 440，首点存在一个尚在解码中的差值，末点均为 10,215。采样错误计数无新增，无硬压力，但这不等于共享磁盘没有其他竞争。旧 eligible cutoff 算法得到的积压增量 25,921 少算了 377，本报告统一使用当前 head 差值。

本地原始证据：`build/benchmarks/20260909-state-cost/baseline-window/`，独立核算 `independent-baseline-recheck.json`。完整前后时间线另见此前的 [状态积压解释](../../build/benchmarks/20260909-state-backlog-reversal/analysis.md)。

## 修复内容

### 清单缓存

单条缓存默认保守驻留计费预算由 64MiB 改为 **256MiB**。`GTRON_SNAPSHOT_MANIFEST_CACHE_BUDGET_BYTES` 接受十进制字节，范围 0–1,073,741,824，0 禁用；空值使用默认。原 `GTRON_SNAPSHOT_MANIFEST_CACHE=0/1` 开关保留，禁用时也检查预算格式，非法配置返回错误。这个值不是进程 RSS 或瞬态解码内存上限。

缓存的私有 Segments/Retired 容器精确分配 len 个元素，按照实际持有容量计费，避免 JSON 解码过程中多分配但缓存并不需要保留的空间导致提前拒绝。输入文件容量、对象、字符串及原有双倍保守系数仍计入。新内容拒绝准入时释放无用旧条目；缩小预算或关闭缓存，即使随后读文件失败也释放超预算条目。

仍每次 `ReadFile` 并比较完整当前字节；不凭路径、mtime、generation 或摘要复用。调用者得到可独立修改的副本，production 校验和独立段文件认证不变。新 generation 仍需要完整解码。去掉原 raw 大小预拦后，超预算大清单也要解码并计算完整候选收费；不声称所有 miss 或极大清单路径都更快。

新增 `state/snapshot/cold/manifest_cache/` 下的 `budget_bytes`、`candidate_charge_bytes`、`headroom_bytes`、`rejections/budget`、`last_rejection_charge_bytes`。结合原 hits/misses/bypasses/resident 判断是否命中与是否接近容量上限，未来超过配置预算仍会拒绝，并非无限缓存。

### 状态批量成本

之前按上一批 `BuildDuration + BeforeMergeDuration` 估算单位块/tx 耗时，其中包含每批都要做的清单工作。清单越大、批量越小，固定成本越容易被误当成行密度上升，导致持续缩批。

本轮只扣除直接计时、明确属于元数据的区域：构建前清单与覆盖规划、清单集成发布，以及生命周期中的可信引用登记、裁剪覆盖规划和清单进度更新。真正的历史/事件构建、段内容认证、数据库扫描、删除、批次提交及未分类的工作仍进入批量时间估算。无法回传或不合法的计时保守回退，不以拟合猜测固定成本。

**完整生命周期恢复仍按照全部实际耗时计算**，元数据并非免计费。字节/块/txnum 限制、失败惩罚、取消恢复、硬压力准入和已有截止保留。增长仍每批最多 25%，小整数范围在实际容量允许时可增长 1；未知压力和硬压力不增长。纯元数据的计时精度边界仍是有效样本，不能重置到初始大批量。

新增 `history/budget/density_work`、`density_metadata_work`、`density_total_work`（纳秒）及 `density_measurement`：0 无元数据测量，1 合法实测，2 无效计时保守回退。它们只解释下一批范围；原 `recovery_cost` 继续表示完整外层维护减去独立计费 merge，不能混用。

### 共享调度审计

没有发现永久占锁、空轮询不断延长状态截止或索引预约不释放的证据。索引预约有 10 秒上限、每叶消费、失去资格撤销。索引加速确实增加共享资源竞争，但现有采样不能精确分摊各任务的等待。本轮不同时取消恢复、降低索引份额或新增并行磁盘任务，先验证消除固定成本后的净追赶。

## 本地证据

Apple M1 Max / darwin arm64，`GOMAXPROCS=4`。固定 25,888 active 与 35,532 retired 引用，仅数量来自生产，路径与校验值为合成。长路径样本专门跨越旧容量边界，不称生产清单。冻结旧加载器关键路径与当前加载器使用相同文件，包含 ReadFile、认证、解码或副本返回；首末完整对象比较一致。三次、每次三个操作，取中位数，短测仅用于确定数量级。

| 样本与路径 | 旧 64MiB | 当前紧凑缓存 256MiB |
|---|---:|---:|
| 普通合成路径，重复加载 | 3.796 ms | 3.789 ms，基本不变 |
| 跨预算长路径，重复加载 | 95.036 ms | 4.419 ms |
| 同长路径累计分配 | 83,391,109 B/op | 22,946,616 B/op，减少 72.48% |
| 同长路径首次冷加载 | 94.354 ms | 94.022 ms，基本不变 |
| 同首次冷加载累计分配 | 83,389,322 B/op | 89,787,677 B/op，增加约 7.67% |

长路径 JSON 为 16,545,825 字节；旧候选收费 69,365,568 字节被拒绝，新私有容器收费 67,346,304 字节成功驻留。第一次解码需要新增私有副本，后续重复加载减少分配。缓存命中收益不能按比例换算到持续发布清单时的整节点吞吐，也不是冷文件压缩率提升。

定向缓存、跨预算大目录正确性通过；小 fixture 并发 race 通过（4.043 秒，包含实际并发发布/读取，未重复大目录 race）。成本及现有 load/catchup/lifecycle 定向通过：snapshots/pruning 2.516/9.197 秒；对应 race 3.444/10.647 秒。真实生命周期测试阻塞实际 `Batch.Write` 后释放，确认提交成本留在密度估算和完整恢复中，且热记录实际删除。

原始日志及参数在 `build/benchmarks/20260909-manifest-budget/` 和 `build/benchmarks/20260909-state-cost/history-cost-*`。两个独立只读审查均未发现阻断问题。后续线上验收以同一进程完整窗口内状态净积压与同步影响为准。

全仓 `go test ./... -count=1 -timeout=300s` 退出 0，54 个有测试包全部通过、13 个无测试包。日志保留在 `build/benchmarks/20260909-state-cost/full-test.txt`。


## 服务器原生验证与切换

代码已提交为 `a869cfb14df6c2d5e6194dd565f203e6ed606036` 并推送，服务器 `git pull --ff-only` 后以该提交归档到独立源码目录，使用现有原生 Sapling 静态库、Go 1.25.5、`-tags sapling` 验证。八个相关包测试全部通过（snapshots 35.661 秒、pruning 8.808 秒），候选构建退出 0。二进制 SHA256 为 `f92d64d87252fd5630bb55a32ce36978a8526d1931cd487e429d26becd7b1017`。

真实生产清单的不可变文件副本：17,857,954 字节，generation 13,299，27,383 active / 37,596 retired。**以下是同一新代码比较两个预算**，每档一次未计时预热后重复加载十次，不包含首次准入，也不是整节点旧新版 A/B。

| 真实副本重复加载 | 64MiB 预算 | 256MiB 预算 |
|---|---:|---:|
| 十次累计耗时 | 3.207 秒 | 0.197 秒 |
| 每次平均 | 320.717 ms | 19.716 ms |
| 十次累计分配 | 916,025,968 B | 246,257,680 B |
| 命中 / 未命中 / 预算拒绝 | 0 / 10 / 10 | 10 / 0 / 0 |
| 驻留收费 | 0 B | 69,729,242 B |

两档首末对象与禁用缓存读取的完整对象一致。候选收费约 66.50MiB，超过旧预算，约占新预算 25.98%。该单组件重复加载约快 16.27 倍、分配减少 73.12%，不能换算为整节点速度或数据库压缩率。

17:05:34 开始正常停止旧 `22972031`，日志确认 clean shutdown；17:05:44 启动候选，17:05:48 服务 ready。新版 PID 3452，实际 `/proc/PID/exe` 为 `/data/gtron/releases/20260909-state-cost-a869cfb1/gtron`，哈希一致，精确 metrics 进程标识 `1788944744841695820`。首次尝试以普通用户调用 systemd，被服务器交互鉴权拒绝，旧进程未停止；保留失败记录，随后使用该账户已有 sudo NOPASSWD 运维权限完成切换。systemd 配置只替换 gtron 可执行路径，数据、参数、端口、其他服务及全局维护 hold 保持原状，主网磁盘 hold 不存在。

服务器原始日志、清单副本、源码身份、二进制、配置备份与切换记录保留在 `/var/tmp/gtron-state-cost-20260909/`。本地 `server-validation.json`、`production-manifest-probe.json` 是终端 UI 读数转录，不能当作下载的服务器原始文件。

未来预警需联合 `candidate_charge_bytes > budget_bytes`、预算拒绝增量与命中率；超限会清空驻留，`headroom_bytes` 可能恢复成整个预算，因此不能单独看剩余空间。每次命中仍有完整文件读取和对象复制；新清单字节发布后的首次加载仍需解码，裁剪 progress 更新也可能改变字节但不增加 generation。不能仅用 generation 增量判断未命中是否合理。


### 历史查询对照

创世附近既有账户基线（2,043 / 2,044 / 2,045 块）在新版再次通过。切换前保存、切换后已经转入正文和索引冷文件的六个完整区块与六个交易回执逐项相等，验证时正文 coverage 14,221,312、索引 coverage/pruned 12,541,952，均越过样本末块。

另外在新版进程内，仅扫描热裁剪前沿后 20,000 块附近的 16 个已固化区块，找到账户在 14,245,208 和 14,245,209 的两次真实余额变化，保存前/后的标量余额及固定区块哈希；裁剪前沿通过后再次验证完全一致。此样本验证新的热转冷路径，创世样本验证既有旧历史。它们是有限 canary，不是所有历史域或全库完整性检查。

本地请求及响应保留于 `build/benchmarks/20260909-state-cost/` 下的 `fresh-cold-*`、`recent-history-*` 与对应 `capture-*` 目录，创世复查在 `genesis-canary.log` 指向的原 canary 目录。


## 线上吞吐验收

北京时间 17:06:59–17:16:59，41/41 点、599.979 秒，同一新版进程。下表积压始终采用当前 `head − published`，不使用可能陈旧的 eligible cutoff；两窗高度、负载和时长不同，并非严格因果 A/B。

| 窗口 | head 块/秒 | 状态发布/裁剪 块/秒 | 真实积压变化 |
|---|---:|---:|---:|
| 旧基线 5 分钟 | 104.786 | 17.113 | +26,298 |
| 新版完整 10 分钟 | 87.665 | 84.370 | **+1,977** |
| 新版前 5 分钟 | 84.425 | 67.007 | +5,225 |
| 新版后 5 分钟 | 90.904 | 101.730 | −3,248 |

全窗 head 15,770,156→15,822,753，状态 14,223,664→14,274,284，积压 1,546,492→1,548,469。每点发布与热裁剪前沿相同。缓存 hits +783、miss +101，命中率 88.575%，预算拒绝与 bypass 增量均为零；最后驻留收费 70,227,700 字节（66.974MiB），在新预算内。两个点的短暂零驻留是新内容解码中间态，不能当持续拒绝。

`density_measurement` 41 点均为 1，逐点 `density_work + density_metadata_work == density_total_work`；完整生命周期恢复没有扣除元数据。重复采样的最近批次中位数由基线 143 增到 1,052 块，此为 gauge 分布，不当作所有实际批次的平均。六类错误计数均无增长，无硬压力。

正文前沿增加 65,536，索引前沿增加 245,760，正文−索引债务减少 180,224。正文整段转换记录耗时 90.070 秒，任务开始早于窗口起点；窗口开头连续约 75 秒的采样看不到状态发布前进，随后恢复。共享 gate 包括整段工作及恢复，不能只看状态运行时的瞬时速度，也不能把整个 90 秒都归入本窗口。索引初始样本处于启动等待，随后首三个预算叶各约 2.2–2.5 秒；没有证据把该停顿归因于索引长叶。

**本轮恢复了缓存准入和较大的状态批次，但完整窗口仍净增 1,977 块，持续追赶尚未得到证明。** 后半段的下降也不足以保证下一个正文转换周期或 20M 高度的表现。独立核算和逐分钟桶在 `post-window/state-window-analysis.*`、`independent-post-comparison.*`。

### 资源、空间及剩余热点

独立资源窗口为 17:07:17–17:17:17（41 点、600.005 秒），与上面的 HTTP 窗口错开约 18 秒；它的 head +52,907、状态 +53,723，积压减少 816，不能混用两个窗口的端点。进程身份不变，采样无错误/告警。

| 资源 | 部署前资源窗口 | 部署后资源窗口 |
|---|---:|---:|
| gtron 平均 CPU 核数 | 4.563 | 3.995 |
| gtron RSS 峰值 | 16.346 GiB | 11.956 GiB |
| 共享盘 busy | 77.14% | 78.41% |
| 共享盘 await | 1.179 ms | 1.105 ms |
| 共享盘队列 | 3.079 | 3.064 |
| 共享盘读 / 写 | 146.17 / 100.41 MiB/s | 127.17 / 96.12 MiB/s |

资源窗口的文件系统可用空间减少 617,320,448 字节。磁盘指标包含 MySQL 和其他服务；期间另有服务器容器构建操作，无法精确分摊影响。CPU/RSS 是 gtron 进程统计，仍不能从不同时段比较推导单一改动的因果收益。

17:24:18 的 `du` 实际分配量：chaindata **91.601GiB**，state-snapshots **155.936GiB**，ancient **88.184GiB**，三者合计 **335.721GiB**；`/data` 可用约 **2.420TiB**。这不是逻辑数据量或本轮压缩率；全部历史仍保留，缓存修复不直接改变文件编码，热裁剪也不等于磁盘立即等量下降。

吞吐和资源窗口结束后，17:20:39 单独采集 30.12 秒 CPU profile，总 CPU 样本 107.53 秒。同一进程的状态生命周期栈占 11.73 秒，其中生产清单读取 3.22 秒、history 构建 2.08 秒、event 构建 2.46 秒（累计栈有嵌套，不能直接加成全部耗时）。事件读源仍含两遍正文/回执读取，history 和 event 在协调层串行。后续优先验证两个独立构建分支的有限并行，以及保留整个生命周期恢复、失败 join 和统一发布；不会仅增加压缩 worker 或取消 I/O 保护。
