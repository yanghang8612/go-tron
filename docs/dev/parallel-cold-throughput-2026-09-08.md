# 多核冷化吞吐优化

本轮目标是让历史冷化持续快于同步，减少正在增长的积压。沿用完整历史和现有文件格式；不删除任何历史版本，不重建当前数据库。

## 改动及原因

- 正文 V2 以完整压缩帧并行 Zstd/CRC，最多 8 个 worker，读取、protobuf 变换和落盘保持顺序。原始输入队列最多 64 MiB，复用缓冲另有 64 MiB；再加生产者当前帧、压缩结果和 encoder 工作区。超大帧排空队列后单独处理，因此这些限额不是整个进程 RSS 上限。
- 事件 V4 payload 采用最多 8 个 worker、按原序落盘，在途 raw/encoded 预算 8 MiB，超大行保留同步路径。构造 payload 时直接投影 Data 和 unknown 字段，减少先 Clone 再删除地址/主题的复制；protobuf schema 扩展时退回原通用投影。
- 事件两遍构建之间复用已验证的块和交易哈希，缓存收费上限 32 MiB / 65,536 块。第二遍仍从 ChainDB 重读完整正文并校验 SHA-256，严格检查 receipt 数量、块号、ID；缓存满时退回原解码路径。不会依赖可变正文缓存，也不额外写一份正文 spool。
- V6 首遍逻辑 key 收集增加精确去重，最多 65,536 条 / 16 MiB key 字节；低命中输入试运行 4,096 次后退出。ETL 仍完成跨缓存重置的排序和去重，所有 posting 和旧值保留。
- throughput 在新鲜引擎及设备观测、无硬压力、恢复/健康等级且 await ≤ 2 ms 时采用 80%/90% 历史目标占空比，默认短恢复下限降为 250 ms。未知、过期、压力及较高延迟沿用原预算；明确设置的更长恢复期、失败退避和既有截止不缩短。仍按完整批次时间/输出字节调节范围，仍通过共享重任务准入。

## 固定输入基准

本地 Apple M1 Max，Go 1.27.1。正文比较 GOMAXPROCS=10；事件和 key 比较 GOMAXPROCS=4。表中为三组结果中位数，fixture 各不相同，收益不可相乘。

| 实验 | 原路径 | 新路径 | 范围 |
| --- | ---: | ---: | --- |
| 64 MiB 正文写段，1→8 worker | 419.87 ms | 148.87 ms | 2.82 倍吞吐，输出均 24,294,184 B |
| 三表完整 Migrate，1→8 worker | 541.34 ms | 245.19 ms | 2.21 倍吞吐，输出均 32,443,879 B |
| 16 MiB 事件 payload 的完整构建，1→4 worker | 90.50 ms | 69.00 ms | 耗时减少 23.8%，输出相同 |
| 事件两遍身份复用，固定单压缩线程 | 127.86 ms | 80.50 ms | 耗时减少 37.0%，输出均 141,672 B |
| 重复 key 首遍 ETL，262,144 次/1,024 唯一键 | 21,233,664 B 输入 | 82,944 B 输入 | 少 99.61%，字典相同 |

完整 Migrate 包括三个表、逐帧校验、fsync、manifest 发布及 live install，但输入是确定性内存数据，未包含真实 Pebble 读取及 protobuf Source/Transform。其累计分配从约 251 MB 增到 489 MB，是用内存和 CPU 换吞吐，不能当作峰值 RSS。事件身份复用 fixture 是真实 MemoryChainDB 96 块 × 32 交易，含合成合约调用及无日志交易；累计分配约 49.11→37.99 MB。

原始结果在本地 `build/benchmarks/20260908-freezer-pipeline/` 与 `build/benchmarks/20260908-cpu-cold/`。上述实验不是主网吞吐承诺。

## 正确性边界及验证

测试覆盖完整文件/索引 checksum、全量及地址/主题查询、全部压缩 codec、borrowed buffer、乱序完成、编码/写入/源读取错误、取消后 worker 清理、超大帧、缓存双限与低命中回退、源正文变更及收据损坏拒绝、genesis 特例。

真实 ancient 回归同时覆盖 V1 文件和 V2 compact 后 V1 已回收的来源：删除全部测试热正文/收据，确认热行不存在，再执行两遍身份缓存构建；正文及索引 checksum 与热源基线一致，查询通过。完整校验、fsync、manifest 与热删除顺序没有改变。

全仓初验指出两处读取边界白名单需要跟随实现更新。已明确登记接受 `*ChainDB` 的身份验证适配器，以及拥有原覆盖检查的内部 worker 构建函数；未关闭或放宽全局审计，`scripts/dev/audit_hot_only_reads.sh` 复验通过。最终 `go test ./... -count=1 -timeout 300s` 通过：54 个有测试的包、13 个无测试的包、0 失败。最终相关 snapshots/freezer 定向 race 也通过；原生部署结果见下方验收记录。

## 线上基线

同一旧进程 11:06–14:57 的近四小时记录：同步 63.58 块/秒，冷化 56.07 块/秒，积压扩大 103,855 块。部署准备期 15:20–15:25 的短窗口则为同步 74.59、冷化 89.05，距离缩小 4,338 块，说明负荷及后台阶段有波动。上线后必须使用包含正文转换的连续窗口衡量，不能只挑选有利区间，或把不同高度的结果称为同输入 A/B。

## 原生构建与部署

实现提交 `8912ebbcd76543b60e1fabef31c9f5c07c014d44` 已推送 GitHub，并在服务器拉取。服务器由该提交的 git archive 构建隔离源目录，使用 Go 1.25.5、CGO、sapling 标签及既有 Sapling 静态库；8 个相关原生包测试全部通过，随后构建成功。服务器原始测试、构建、身份与部署记录保存在 `/var/tmp/gtron-cpu-cold-20260908/`。

2026-09-08 15:49:44（北京时间）以新二进制接续现有数据库，PID `19995`，`process/start/unix_nano=1788853784084504777`。`/proc/19995/exe` 核对为 `/data/gtron/releases/20260908-cpu-cold-8912ebbc/gtron`，SHA-256 为 `b68ca44a879fd5155678cb069dfb5d01fa7e4f4cc821398da18ebfef4df490fa`。旧进程正常退出，原服务参数及空间保护保留。未修改 Java、MySQL、Nile 服务。

重启后首次历史 canary 通过：同一账户在 2043、2044、2045 块的区块哈希及三个不同余额均与既有基线一致。该检查只验证一个账户标量历史，不能替代全历史所有域的验证；更广泛的 codec、事件查询及 ancient 来源由上述测试覆盖。

## 连续线上验收

北京时间 15:56:18–16:06:18，连续 41 点、600.0075 秒，同一新进程。没有剔除正文转换造成的暂停。

| 指标 | 起点 | 终点 | 窗口变化 |
| --- | ---: | ---: | ---: |
| 同步高度 | 8,605,459 | 8,652,201 | +46,742，77.90 块/秒 |
| 状态冷化发布 / 热变更清理 | 6,361,123 | 6,427,925 | +66,802，111.34 块/秒 |
| head 与冷化边界距离 | 2,244,336 | 2,224,276 | 减少 20,060 |

冷化相对上线前较好的 5 分钟窗口 89.05 块/秒高约 25%，相对旧版近四小时的 56.07 高约 99%。各窗口高度、工作负荷和缓存温度不同，这些是观察到的吞吐差异，不是同输入因果 A/B。扣除正常 65,536 热窗口、按 solidified 计算，仍有 2,158,722 块额外欠账；不能称已经清空积压。

38/41 样点 `cpu_burst=1`：26 点为 80% 目标占空比、12 点为 90%，其余 3 点压力回退到 20%。这是采样比例，不能当作时间占比。窗口内完成 62 次历史构建、3 次合并；冷化、裁剪、lifecycle、正文 V2、交易索引和裁剪校验错误没有增加。

正文完成两段，每段 65,536 块：`[6,291,456, 6,356,992)` 耗时 83.99 秒，`[6,356,992, 6,422,528)` 耗时 60.86 秒。两次完成日志分别为 16:00:46.387 和 16:05:58.085，单段耗时由捕获各次完成的中间/末尾 metrics 交叉确认。状态发布期间有约 105 秒和 75 秒的采样静止区间，随后均恢复；这些区间还包含调度、清理、恢复及采样误差，不等同于正文压缩函数耗时。

必须分别看各类欠账：完整 metrics 首尾的正文 V2 coverage 增加 131,072，整段 backlog 从 35 段降至 34 段；交易索引只推进 16,384 块，债务反而从 4,784,128 增至 4,898,816。此次状态历史吞吐改善，并没有解决交易索引追赶。正文的 30 秒轮询共享 gate 仍没有公平队列，中间观测到 6 次已就绪候选被 gate 拒绝；不能因此推断永久饥饿，也不能将按 solid 计算的全部正文 backlog 当作事件覆盖已满足的可立即开工数据。

资源采样为 15:56:32–16:06:32，同 PID 连续 41 点：平均 CPU 3.98 核（机器 16 核），RSS 峰值 14.24 GiB；共享数据盘 busy 76.37%、平均 queue 3.13、await 0.944 ms，累计读 77.99 GiB / 写 41.43 GiB。收益包括减少重复工作和等待，不能称整窗已用满 8 个压缩 worker。磁盘指标包括同盘其他服务；资源窗口比 frontier 窗口晚约 14 秒。原始内核资源 JSONL 保留在服务器，服务器生成的摘要经终端读取转录到本地。

首尾 published=pruned，40/41 样点一致；一次跨模块非原子 gauge 读取偏差在下一点消失。16:07:19 再次历史 canary 通过，覆盖两段正文完成后的查询；仍仅限一个账户的三个历史余额/区块哈希。部署日志截至 16:08:26 无 WARN/ERROR/CRIT。完整 metrics 中投机执行 `versioned_shadow/vm_sender_chain` 回退诊断增加 562，与冷化错误属于不同路径，不能笼统写所有错误类计数为零。

此次保持冷文件格式和历史语义，固定输入测试输出大小一致；目标是加快冷化、清理重复热数据，不能把这轮提速当作额外的冷文件压缩率收益。原始前后窗口、两次历史 canary、CPU profile、完整 metrics 与独立核验报告在本地 `build/benchmarks/20260908-cpu-cold/`；服务器完整日志、原生测试、资源采样及部署身份记录在 `/var/tmp/gtron-cpu-cold-20260908/`。

## 验收结束时的空间

16:08 的运行中 `du` 分配空间扫描：

| 目录 | 实际占用 | 占比 |
| --- | ---: | ---: |
| chaindata | 102.07 GiB | 61.60% |
| state-snapshots | 41.36 GiB | 24.96% |
| ancient | 22.26 GiB | 13.44% |

合计 **165.69 GiB**；共享数据盘可用 **2.59 TiB**，现有空间保护检查通过。运行中目录扫描非原子，不能和不同高度、不同后台阶段的旧值直接计算压缩率；新同步数据继续产生，冷化吞吐变快不等于整个数据库立即缩小。
