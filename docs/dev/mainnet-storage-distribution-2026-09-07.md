# 主网维护后存储分布

2026-09-07 07:43:04 UTC（北京时间 15:43:04）只读复测。未启动 gtron，四个同步/部署 unit 均 inactive，停机 hold 存在。前后 `du` 完全相同，manifest generation 10716 和 SHA 均未变。

复用已审计的独立 Pebble helper，执行前核对源文件 SHA-256 `09ad201fed851823b997c4e765258dbbdb12a719c2f7e812776967e69c2e8a6a`。工具设置 ReadOnly、ErrorIfNotExists、DisableAutomaticCompactions；没有节点、P2P、同步依赖。退出码 0。helper 加后续核对耗时 16.69 s，非同步性能基准。

原始 Pebble JSONL 位于服务器 `/var/tmp/gtron-format-lab-20260907/distribution-post-maintenance.jsonl`。本地[核心转录](../../build/benchmarks/20260907-history-format/distribution-transcribed.json)明确标注为读屏转录，并非下载的原文件。

## 总量

下表是 `du -sx -B1` 的分配字节；GiB = 2³⁰ B。占比以这三个主要数据目录之和为分母。

| 目录 | GiB | 占三个目录合计 |
| --- | ---: | ---: |
| `chaindata` | 1,439.41 | 54.83% |
| `state-snapshots` | 988.99 | 37.67% |
| `ancient` | 196.70 | 7.49% |

## state-snapshots 内部

对 manifest 全部活动引用逐文件 stat，核对文件大小并按 device/inode 去重分配块。占比为该类别已分配字节 / 快照目录 du；包含压缩后的实际文件，不是解压内容比例。没有重新扫描所有冷文件的 SHA/语义。

| 类别 | 内容 | GiB | 占快照目录 |
| --- | --- | ---: | ---: |
| `history` | 历史正文：账户、合约存储、委托、系统状态等每次变化前的旧值 | 804.50 | 81.35% |
| `accessor` | 按逻辑 key 查找历史版本及记录位置的字典和 postings | 88.43 | 8.94% |
| `inverted` | 按 txNum 范围定位历史记录的索引 | 8.33 | 0.84% |
| `event-log` | 交易执行产生的合约事件日志，不是 gtron 应用日志 | 76.01 | 7.69% |
| `event-log-index` | 配套事件查询索引 | 11.70 | 1.18% |

历史正文及两个配套索引合计占 **91.13%**。其他约 0.03 GiB 为目录分配和元数据等差额。

## chaindata 内部

这次重新读取 SST 元数据，用 Pebble EstimateDiskUsage 按 key 前缀估算；分母为同次 chaindata du。包括 SST 内旧版本、被遮蔽值和 tombstone，不含 WAL，边界可能有小幅误差。下表用于说明物理范围构成，不能当作唯一 live KV 字节或可释放容量。各项不强行补齐 100%。

| 数据族 | 内容 | 估算 GiB | 占热库 |
| --- | --- | ---: | ---: |
| 状态变更历史 | 账户、KV 等历史原像，供历史状态查询及回滚 | 1,260.77 | 87.59% |
| Commitment branch | 状态承诺树的分支节点 | 62.43 | 4.34% |
| 历史 posting | 具体 key 到历史变更位置的索引 | 37.96 | 2.64% |
| 热区块 | 尚在热库的区块及交易 | 35.53 | 2.47% |
| 最新 KV | 当前合约存储、委托、资源及系统状态等 | 14.21 | 0.99% |
| 热交易索引 | 交易哈希到区块/位置的索引 | 9.06 | 0.63% |
| 历史 key directory | 历史 key 的目录，支持前缀查询 | 7.50 | 0.52% |
| 按块交易回执 | 交易执行结果及回执数据 | 5.96 | 0.41% |
| 最新账户 | 当前账户主体记录 | 2.87 | 0.20% |
| StateTxRange | 区块和全局交易序号范围的映射 | 1.32 | 0.09% |
| 暂存同步区块 | 已接收、待执行的区块 | 0.28 | 0.02% |
| 合约代码 | TVM 字节码 | 0.16 | 0.01% |

最新账户和最新 KV 合计约 **17.08 GiB、1.19%**；历史原像约 **1,260.77 GiB、87.59%**。维护前的 1,352.67 GiB / 88.28% 已不适用于当前热库。

首条 live changeset 为 block 29,127,573，与已发布 hot-prune block 29,127,572 衔接。冷历史和热历史存放不同阶段的版本；并非这两份大目录全部互为重复副本。要继续从热库移除数据，必须先验证冷覆盖，且冷文件新增占盘也要计入净收益。

本表确定了全库文件族的大头，尚未把全部历史正文按账户/合约/委托进一步完整拆分。此前 32 块样本中 drax-0 占旧值字节 95.87%，只对该样本成立，不能应用于整库。压缩实验及其限制见[实验报告](history-format-experiments-2026-09-07.md)。
