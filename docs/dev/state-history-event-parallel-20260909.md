# 状态历史与事件日志的双分支构建

本轮接续 `a869cfb1` 的缓存及批次成本修复。该版本完整十分钟状态84.370块/秒、同步87.665块/秒，积压仍净增1,977块；因此继续缩短同一状态批次的关键路径。保留当前数据库和全部历史，不改变文件格式或查询语义。

## 实现边界

同一已固化范围的状态历史和V4事件日志读取不同的数据，没有新文件依赖。本轮在原共享维护lease内并行两个原有builder，各自的压缩worker上限保持不变。两边退出、全部构建成功且父任务尚未观察到取消后，才统一组装manifest发布，后续严格校验和热裁剪顺序保持。

只在吞吐同步模式、必需事件侧车、新鲜健康的存储观测、至少8个Go调度线程，以及额外CPU/内存探针全部允许时启用。其他情况保持串行；串行构建成功后重新观察同步状态，保留原来的balance/section侧车延期行为。既有内部扫描不能即时抢占，取消时等待当前有界工作全部退出，不提前释放lease。未发布内容地址文件沿用原孤儿安全处理；不能盲删可能被旧manifest引用的同名文件。manifest已发布后的阶段写入失败仍保留文件，不能将所有错误概括为未发布。

使用现有生产并发安全的Pebble和完整ChainDB读接口，保留ancient回退和external-log还原。同步暂存cleanup使用不同键前缀；正文转换与对应热删除受同一lease保护，历史裁剪依赖Runner的有序发布与验证覆盖。没有新增MVCC或伪装的可写快照适配器。此结论限于当前生产接线和固定已固化范围。

## 资源准入

探针懒采样，不启动后台任务或扫描数据库。有调用时最多每5秒重新读取有界proc/cgroup文件，CPU取间隔5–30秒的两点平均。只统计亲和性允许的CPU，guest不重复计数，iowait与steal不作为空闲；从min(GOMAXPROCS、NumCPU、允许CPU数)中扣除观察到的忙核，避免GOMAX较小时高估余量。动态调低GOMAX立即收紧；有限CPU quota保守串行，不从宿主闲核推断cgroup额度尚有剩余。

启用门槛为实际idle至少25%、剩余执行能力至少2核，以及至少2GiB内存余量。内存取MemAvailable和各可见cgroup父层硬/软限制减用量的最小值。首次、陈旧、时间/计数回退、范围变化、慢读取、缺失或截断均拒绝并行。资源提示仅用于准入，不能保证整个批次的负载或峰值内存。

服务器已核为16核、普通cgroup v1、gtron.service硬上限40GiB、CPU quota无限；本轮按该环境原生验证。私有cgroup namespace可能隐藏宿主祖先，不能从可见挂载根推导任意容器的全部限制。

实际运行指标为 `state/snapshot/cold/history/event_parallel/` 下的 attempts、builds、last/paired、last/build_wall、last/history、last/event。last仅在真正获准历史构建时更新，延期轮次不覆盖；分支耗时可以重叠，批量密度和完整恢复继续使用实际总墙钟。探针指标位于 `history/parallel/runtime/`，ready不能替代实际并行计数。

## 本地验证

真实Pebble定向测试通过2.418秒，对应race通过3.806秒；资源探针定向通过1.152秒，race通过2.087秒。覆盖实际双源重叠、失败/取消时等待另一分支且lease保持、manifest/stage/prune不提前推进、失败重试、预存输出不被误删、发布后阶段失败保留文件、串行idle→sync时序与错误优先级。串并行全部文件引用/checksum、完整状态变化和事件查询相等，包括protobuf未知字段；并发同步暂存写删通过race。

受控同输入基准：Apple M1 Max，Darwin arm64，实际10核，显式GOMAXPROCS=16、event worker8。256块×32交易，共8,192交易、16,384状态变化，prev/log数据各2KiB；包括真实读取、两遍扫描、压缩、校验、fsync、统一发布及阶段写入。每次新清单，没有模拟生产的大存量清单和同步/正文转换争用。

| 三次中位数 | 串行 | 双分支 |
|---|---:|---:|
| 完整OnePass | 265.817ms | 215.878ms（耗时减少18.8%） |
| 内层BuildDuration | 260.108ms | 208.607ms（耗时减少19.8%） |
| 输出大小 | 51,287,680B | 51,287,680B |

所有输出引用和checksum相等。各分支因争用略变慢，总耗时因重叠而减少；不能承诺理想35%减时，也不能将累计分配B/op当作峰值RSS。

命令：`GOMAXPROCS=16 go test ./core/state/snapshots -run '^$' -bench '^BenchmarkParallelHistoryEventFullBuild$' -benchtime=1x -count=3 -timeout=180s`。正式本地原始证据在 `build/benchmarks/20260909-parallel-history-event/{targeted,race,benchmark-gomax16}.txt`；资源探针证据在上一轮 `20260909-state-cost/parallel-runtime-probe-*`。两位独立审查完成，已修正串行规划时序和CPU有效余量的计算。

全仓测试先在沙箱通过44个包、13个包无测试，10个需本机监听端口的包被权限拒绝；允许临时本机监听后重跑这10包全部通过，合并为54包通过、13包无测试。原始失败与重跑记录均保留，没有将第一次执行记为成功。

## 服务器原生验证

提交 `9059eb7fabc8caefd584af70ed3e1236137b07f3` 从 git archive 单独构建，使用现有原生 Sapling 静态库。Go 1.25.5、Linux amd64，8个相关包的 `-tags sapling` 原生测试及二进制构建均退出0。候选二进制 SHA-256 为 `2d3f9707c176c3e15b30d492d676e8594b483200a9569912b649ef7407bf4991`。

同输入基准在 Intel Xeon Platinum 8175M、GOMAXPROCS=16、event worker8下单独执行，三次完整OnePass分别为串行587.336/544.326/578.067ms，并行286.716/288.178/296.002ms，中位数 **578.067 → 288.178ms，耗时减少50.1%**。内层BuildDuration中位数573.809 → 283.073ms。所有输出引用及checksum相等，输出大小均51,287,680B。该基准仍是固定小清单夹具，不替代线上长窗口。

服务器原始证据保存在 `/var/tmp/gtron-parallel-history-event-20260909/` 的 `source-identity.json`、`native-tests.log`、`native-build.log`、`native-benchmark.log` 和对应exit文件。本地 `native-validation-transcription.json` 是从独立终端可见输出转录的摘要，不是服务器原始日志副本。上线积压结果另行记录。

## 上线与查询验证

北京时间18:03:19，部署脚本正常停止 `a869cfb1`（PID3452）后启动 `9059eb7f`，退出0；只修改现有service的二进制路径，空间保护检查通过、全局维护hold保持。新PID25232，start_ticks为4468269266，网关进程标识为 `1788948199153245090`；实际 `/proc/PID/exe` 指向本次release，SHA-256与候选一致，systemd active/running、Result success、ExecMainStatus 0。实际 `event_parallel/builds` 已增长。锁屏曾短暂阻止终端核验，恢复后已补齐；HTTP采样保持连续，资源采样另起独立10分钟窗口，不混合两组端点。

切换前第一次回执基线采集中的一个索引样本请求曾返回 `pebble: not found`。原脚本没有保存该交易及块号，原因仍未确认，不能宣称已修复，也不能归因于本轮新代码。日志保留在 `fresh-capture.log`；补充的有界逐请求留证采样获得6块和6份回执，14次RPC均成功且未重试。最初固定16块余额采样未找到两次变化，随后另一个固定16块范围使用重复交易sender作为候选提示，仍保留原先全部余额变化和区块hash断言，首个候选成功。

上线后，账户 `0x880e4776dfcf38dfe00d399d31738abc773634e8` 在14,531,440/441/442块的余额及canonical hash逐项一致，两次变化分别为−64,210/−28,770 SUN。固定14,530,722–14,530,737区间的545条事件日志及边界区块hash全部一致。新进程第一个完整窗口样本的状态前沿14,529,691尚未覆盖这些区间，验证时已越过，因此这两项确实经过本轮新版本冷化。另有既有创世早期2043/2044/2045块标量余额基线通过。它们是有界查询回归，不能表述为全库或全部历史域审计。

正文/索引样本在18:10:22验证通过，完整6块及6份回执逐项相等。新进程首个窗口样本正文覆盖仍14,483,456，随后推进至14,548,992，三份正文样本14,483,456/14,516,224/14,548,991确实经过新版本迁移。索引样本的迁移可能在切换前已经发生，只作为跨重启查询回归，不算本轮新增索引迁移证明。

进度指标并非原子快照。`cold_builder.go`先持久化发布manifest，再调用生命周期的PrunePass，pruner先导出热裁剪进度，而cold published gauge要等整轮recordPass才更新。因此采样可能短暂出现prune领先published。实际裁剪读取生产manifest并验证覆盖与文件内容，不用published gauge作删除边界。例如第39点领先1,596块，第40点published追平、构建计数增加1且批次正好1,596块。统计保留原始倒差，不强行归零，也不将它当作越界删除证据。

## 完整15分钟线上窗口

北京时间18:05:24.086–18:20:24.051，61/61点、实际单调时钟900.015250688秒，始终同一新进程；采样退出0。原始HTTP结果共1,143,035字节，严格分析位于本地 `build/benchmarks/20260909-parallel-history-event/post-window/parallel-window-analysis.{json,md}`。

| 指标 | 开始 | 结束 | 完整窗口变化 |
|---|---:|---:|---:|
| 同步头 | 16,108,713 | 16,208,276 | +99,563；110.624块/秒 |
| 状态发布/热裁剪 | 14,529,691 | 14,642,893 | +113,202；125.778块/秒 |
| head减状态发布 | 1,579,022 | 1,565,383 | **−13,639块** |
| 正文覆盖 | 14,483,456 | 14,614,528 | +131,072 |
| 交易索引已裁剪覆盖 | 13,819,904 | 14,073,856 | +253,952 |
| 正文减索引已裁剪 | 663,552 | 540,672 | −122,880 |

68次成功历史构建中55次实际并行，比例80.88%；不能用探针ready的47/61个采样点或last/paired的45/61点替代实际执行比例。cold/prune/body/index/lifecycle/checksum六类错误增量均0，hard压力0/61、device known61/61。预算等级1/2/3分别13/40/8点，期间软压触发串行/缩批后自行恢复并行。

| 连续5分钟 | 同步块/秒 | 状态块/秒 | 净积压变化 |
|---|---:|---:|---:|
| 前5分钟 | 114.788 | 102.874 | +3,574 |
| 中间5分钟 | 100.265 | 136.705 | −10,933 |
| 后5分钟 | 116.819 | 137.753 | −6,280 |

包含两轮各65,536块正文发布，其last batch墙时37.818/20.108秒。附近状态发布不变的采样区间分别约59.934/45.196秒，期间维护轮次与resource deferral同步增长，随后恢复；这些区间包含让行、恢复等，不能等同正文编码墙时。5个采样点有prune领先published，各自在下一点收敛；窗口两端均相等，净积压端点计算不受影响。

这证明本窗口进入净消化，但仍有1,565,383块head到状态前沿距离，扣除正常热窗口/固化差额后额外债务约1,499,829块，远未清零。中间一分钟导入只有39.81块/秒，贡献了部分净缩减，不能将整窗结果全部归因并行，也不能声称同步零影响。与上一轮不同高度、密度及窗口长度的观测不是严格A/B；不能外推20M或永久追赶速度。后5分钟在同步116.819块/秒时状态137.753块/秒仍净消化，是当前较稳妥的有效性证据。

## 资源、磁盘及剩余限制

独立资源窗口为18:13:38.113–18:23:38.121，实际600.008244秒、41/41点，同PID25232/start_ticks4468269266；严格汇总通过，无采样错误或告警。因锁屏导致该窗口稍后开始，不能将其端点与上面15分钟窗口混用。

| 资源 | 本轮独立10分钟 | 上一轮a869独立10分钟 |
|---|---:|---:|
| gtron平均CPU核数 | 4.252 | 3.995 |
| gtron峰值RSS | 15.011GiB | 11.956GiB |
| 共享磁盘忙碌率 | 78.687% | 78.413% |
| 平均I/O等待 | 1.086ms | 1.105ms |
| 平均队列深度 | 3.029 | 3.064 |
| 读取/写入 | 132.007 / 90.229MiB/s | 127.165 / 96.117MiB/s |

并行会同时驻留两个builder的数据，本次RSS有所增加，低于现有40GiB服务硬上限。上述共享盘包含MySQL和其它进程，窗口也不是同输入A/B，不能把全部变化归给gtron。资源窗口内head推进69,475、state推进90,067，净积压另减20,592块（115.790/150.110块/秒）；这是另一组时间端点，不替换主HTTP窗口的−13,639。共享卷可用空间该窗口增加438,173,696字节，不能独立归因于gtron回收。

18:25:15，在线只读du完整退出0，无文件消失错误；实际已分配磁盘空间如下，在线遍历本身不是原子数据库快照：

| 路径 | 已分配字节 | GiB |
|---|---:|---:|
| chaindata | 93,634,973,696 | 87.204 |
| state-snapshots | 173,297,561,600 | 161.396 |
| ancient | 98,346,184,704 | 91.592 |
| gtron数据库目录总计 | 365,278,724,096 | 340.192 |

`/data`可用2,655,210,946,560字节，约2.415TiB。本轮调度/缓存改动不改变存储格式与同输入文件大小，不能把热库下降称为压缩率提升；保留全部历史、持续导入时，总历史数据仍会增长。

服务器资源原始数据及摘要为本轮目录下 `post-resources.jsonl`、`post-resources-summary.json`，空间证据为 `disk-du.stdout`、`disk-du.stderr`、`disk-sizes.json`。本地对应transcription文件明确标注是终端转录。自部署起至18:21:06的有界日志84,127字节、207行，ERROR/CRIT/panic/fatal及WARN匹配均0；旧进程18:03:11.228记录Clean shutdown，新节点18:03:22.834记录gtron started。

两轮优化已上线，固定成本及串行关键路径均有可核验改善；积压清零仍未完成。后续主要约束是正文维护让行、Pebble软压力导致的降级，以及随区块高度增加的数据密度。保留这些保护和全部历史语义，不以提高并行计数本身替代整轮净消化和资源验证。
