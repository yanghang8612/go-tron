# 热历史范围清理调度实施计划

1. 复核现网版本、零命中与完整调用链；区分“可能持 lease”的 API 约束和实际调用方。
2. 增加进程级、按尝试即时发布的原因计数；保留旧 Try API，新增可排队入口及计时。
3. Worker 同一 pass 内共享 256 块排队额度，64 块一片；新增独立开关和生产适配。
4. 运行锁竞争/取消/证明/水位/额度定向与 race、受影响包测试及增量 lint，独立审查。
5. 冻结 source 和独立 ops、native prepared、旧 PID/ticks/二进制/配置，GitHub 推送部署。
6. 完成 21 点前后采样、实际命中/持锁分析、canonical 抽查、allocated du 与配置末检。

完成状态和现场证据写入本轮部署报告；计划本身不表示实现、测试或部署已完成。
