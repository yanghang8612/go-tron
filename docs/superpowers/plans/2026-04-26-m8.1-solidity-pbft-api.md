# M8.1 Solidity / PBFT API Variants — Implementation Plan

Date: 2026-04-26  
Spec: `docs/superpowers/specs/2026-04-26-m8.1-solidity-pbft-api-design.md`

## Tasks

- [ ] PR-1: Backend interface + solid/pbft block num methods
  - Add `SolidifiedBlockNum() uint64` and `LatestPbftBlockNum() int64` to `tronapi.Backend` interface
  - Implement in `core/tron_backend.go`
  - Add stub methods to `internal/tronapi/api_test.go`
  - Verify: `go build ./...` passes

- [ ] PR-2: HTTP walletsolidity + walletpbft routes
  - Create `internal/tronapi/solidity.go`: `RegisterSolidityRoutes(mux)` + solid-aware getNowBlock/getBlockByNum/getTransactionInfoByBlockNum
  - Create `internal/tronapi/pbft_api.go`: `RegisterPbftRoutes(mux)` + PBFT-aware variants (fallback to solid if PBFT not active)
  - Modify `internal/tronapi/api.go`: call both register functions from `RegisterRoutes`
  - Verify: `go build ./...` passes

- [ ] PR-3: gRPC WalletSolidity service
  - Create `internal/grpcapi/solidity.go`: `SolidityServer` implementing `WalletSolidityServer` (~30 methods, shielded → Unimplemented)
  - Modify `internal/grpcapi/server.go`: register `SolidityServer` in `Start()`
  - Verify: `go build ./...` passes

- [ ] PR-4: Tests + PLAN.md update
  - Create `internal/tronapi/solidity_test.go`: solid/pbft variant routing tests (at minimum: solid getNowBlock returns solid block, PBFT fallback works)
  - Create `internal/grpcapi/solidity_test.go`: WalletSolidity gRPC test via bufconn
  - Run `make test` — all packages green
  - Update PLAN.md: mark M8.1 complete; flip G2 gate if all M4+M5 deps satisfied

## Verification

```bash
make test                          # all packages green
go vet ./internal/... ./core/...   # clean
```
